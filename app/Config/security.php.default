<?php

// A random string used in security hashing methods.
$config['Security']['salt'] = 'CHANGE ME';

// A random numeric string (digits only) used to encrypt/decrypt strings.
$config['Security']['cipherSeed'] = 'CHANGE ME';
